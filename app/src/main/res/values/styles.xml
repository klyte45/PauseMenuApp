<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="windowNoTitle">true</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowFullscreen">true</item>
    </style>

    <style name="default_input">
        <item name="android:textSize">@dimen/mainMenu.listItem.fontSize</item>
        <item name="android:textColor">@color/defaultMenuItemColor</item>
        <item name="android:textCursorDrawable">@drawable/text_caret_1980</item>
    </style>

</resources>
